import 'package:flutter/material.dart';

class TailerScreen extends StatelessWidget {
  const TailerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}